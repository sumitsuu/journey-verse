model User {
  id             Int     @id @default(autoincrement())
  email          String  @unique
  displayName    String
  password       String
  avatarPath     String?
  favouredTypeId Int?
  favouredType   Type?   @relation(fields: [favouredTypeId], references: [id])
}
